#include "kcl.h"
#include "timer.h"
//#include <vector>
//#include <algorithm>

// Compares with simple serial implementation that uses std::set_intersection
void KCLVerifier(Graph &g, unsigned k, uint64_t test_total) {
  //printf("Verifying...\n");
  //uint64_t total = 0;
  Timer t;
  t.Start();
  t.Stop();
  //printf("\truntime [serial] = %f ms.\n", t.Millisecs());

  //if(total == test_total) printf("Correct\n");
  //else printf("Wrong\n");
  //printf("total=%lld, test_total=%lld\n", total, test_total);
  return;
}
